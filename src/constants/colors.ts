// src/constants/colors.ts

/**
 * Stat-specific colors (not theme colors)
 */
export const STAT_COLORS = {
    STREAK: '#FF9F43',  // Orange for streak indicator
    MINUTES: '#54A0FF', // Blue for minutes indicator
} as const;

/**
 * Common color values
 */
export const COMMON_COLORS = {
    BLACK: 'black',
    WHITE: 'white',
    TRANSPARENT: 'transparent',
} as const;
